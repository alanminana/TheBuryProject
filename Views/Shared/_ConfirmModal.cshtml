@*
    Modal de confirmación Bootstrap reutilizable.
    Se activa automáticamente desde elementos con atributo data-confirm.
    
    Atributos opcionales:
    - data-confirm="mensaje": El mensaje principal de confirmación
    - data-confirm-title="título": Título del modal (default: "Confirmar acción")
    - data-confirm-variant="danger|warning|primary": Variante del botón (default: "primary")
    - data-confirm-confirm-text="texto": Texto del botón confirmar (default: "Confirmar")
    - data-confirm-cancel-text="texto": Texto del botón cancelar (default: "Cancelar")
*@

<div class="modal fade" id="confirmModal" tabindex="-1" aria-labelledby="confirmModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-0 shadow">
            <div class="modal-header border-0 pb-0">
                <h5 class="modal-title d-flex align-items-center gap-2" id="confirmModalLabel">
                    <i class="bi bi-question-circle text-primary" id="confirmModalIcon"></i>
                    <span id="confirmModalTitle">Confirmar acción</span>
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
            </div>
            <div class="modal-body py-4">
                <p id="confirmModalMessage" class="mb-0"></p>
            </div>
            <div class="modal-footer border-0 pt-0">
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal" id="confirmModalCancelBtn">
                    Cancelar
                </button>
                <button type="button" class="btn btn-primary" id="confirmModalConfirmBtn">
                    Confirmar
                </button>
            </div>
        </div>
    </div>
</div>
