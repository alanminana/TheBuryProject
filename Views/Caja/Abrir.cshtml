@model TheBuryProject.ViewModels.AbrirCajaViewModel

@{
    ViewData["Title"] = "Abrir Caja";
}

<section class="container-fluid px-0 py-3 text-light">

    <!-- Header de página -->
    <div class="d-flex align-items-center justify-content-between mb-4">
        <div>
            <div class="text-body-secondary text-uppercase small mb-1">Cajas</div>
            <h1 class="h4 mb-1 d-flex align-items-center gap-2">
                <i class="bi bi-unlock" aria-hidden="true"></i>
                <span>Abrir caja</span>
            </h1>
            <p class="text-body-secondary small mb-0">
                Define el monto inicial y deja registrada la apertura para los próximos movimientos.
            </p>
        </div>

        <a asp-action="Index" class="btn btn-outline-secondary btn-sm">
            <i class="bi bi-arrow-left me-1" aria-hidden="true"></i>Volver al listado
        </a>
    </div>

    <div class="row">
        <div class="col-12 col-lg-7 col-xl-5">

            <div class="card card-animate">
                <div class="card-body">

                    <form asp-action="Abrir" method="post" novalidate>
                        @Html.AntiForgeryToken()

                        @if (!ViewData.ModelState.IsValid)
                        {
                            <div asp-validation-summary="ModelOnly" class="alert alert-danger mb-3" role="alert"></div>
                        }

                        <!-- Caja -->
                        <div class="mb-3">
                            <label asp-for="CajaId" class="form-label"></label>
                            <select asp-for="CajaId"
                                    class="form-select"
                                    asp-items="ViewBag.Cajas"
                                    aria-describedby="cajaHelp">
                                <option value="">-- Seleccione una caja --</option>
                            </select>
                            <div id="cajaHelp" class="form-text">
                                Selecciona la caja que vas a abrir.
                            </div>
                            <span asp-validation-for="CajaId" class="text-danger small"></span>
                        </div>

                        <!-- Monto inicial -->
                        <div class="mb-3">
                            <label asp-for="MontoInicial" class="form-label"></label>
                            <div class="input-group">
                                <span class="input-group-text" aria-hidden="true">$</span>
                                <input asp-for="MontoInicial"
                                       type="number"
                                       step="0.01"
                                       min="0"
                                       inputmode="decimal"
                                       class="form-control"
                                       placeholder="0,00"
                                       aria-describedby="montoInicialHelp" />
                            </div>
                            <span asp-validation-for="MontoInicial" class="text-danger small d-block"></span>
                            <div id="montoInicialHelp" class="form-text">
                                Monto en efectivo con el que inicia la caja.
                            </div>
                        </div>

                        <!-- Observaciones -->
                        <div class="mb-3">
                            <label asp-for="ObservacionesApertura" class="form-label"></label>
                            <textarea asp-for="ObservacionesApertura"
                                      class="form-control"
                                      rows="3"
                                      placeholder="Observaciones opcionales..."></textarea>
                            <span asp-validation-for="ObservacionesApertura" class="text-danger small"></span>
                        </div>

                        <!-- Info -->
                        <div class="alert alert-info mb-3" role="alert">
                            <i class="bi bi-info-circle me-1" aria-hidden="true"></i>
                            <strong>Importante:</strong>
                            Una vez abierta la caja, se registrarán todos los movimientos hasta su cierre.
                        </div>

                        <!-- Botones -->
                        <div class="d-flex justify-content-end gap-2">
                            <a asp-action="Index" class="btn btn-outline-secondary">
                                <i class="bi bi-x-circle me-1" aria-hidden="true"></i>Cancelar
                            </a>
                            <button type="submit" class="btn btn-success">
                                <i class="bi bi-unlock me-1" aria-hidden="true"></i>Abrir caja
                            </button>
                        </div>

                    </form>

                </div>
            </div>

        </div>
    </div>
</section>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
