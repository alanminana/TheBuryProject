@using TheBuryProject.Models.Entities
@model TheBuryProject.ViewModels.CrearSolicitudAutorizacionViewModel

@{
    ViewData["Title"] = "Nueva Solicitud de Autorización";
    var returnUrl = ViewData["ReturnUrl"] as string;
    var backUrl = returnUrl ?? Url.Action("Solicitudes", "Autorizacion")!;
}

<section class="container-fluid px-0 py-3 text-light">

    <!-- Header -->
    <div class="d-flex flex-wrap align-items-start justify-content-between gap-2 mb-4">
        <div>
            <div class="text-muted text-uppercase small mb-1">Autorizaciones</div>
            <h1 class="h4 mb-1 d-flex align-items-center gap-2">
                <i class="bi bi-send" aria-hidden="true"></i>
                <span>Nueva solicitud de autorización</span>
            </h1>
            <p class="text-muted small mb-0">
                Solicite autorización para exceder los límites configurados para su rol.
            </p>
        </div>

        <a href="@backUrl" class="btn btn-outline-secondary btn-sm">
            <i class="bi bi-arrow-left me-1" aria-hidden="true"></i>Volver a solicitudes
        </a>
    </div>

    <!-- Info -->
    <div class="alert alert-info mb-4" role="alert">
        <i class="bi bi-info-circle me-2" aria-hidden="true"></i>
        <strong>Información:</strong>
        Complete este formulario para solicitar autorización cuando una operación excede los límites establecidos para su rol.
        Un supervisor revisará su solicitud.
    </div>

    <div class="row g-3">
        <div class="col-12 col-lg-8 col-xl-6">

            <div class="card card-animate">
                <div class="card-body">

                    <form asp-action="CrearSolicitud" method="post" novalidate>
                        @Html.AntiForgeryToken()
                        @if (!string.IsNullOrWhiteSpace(returnUrl))
                        {
                            <input type="hidden" name="returnUrl" value="@returnUrl" />
                        }

                        @if (!ViewData.ModelState.IsValid)
                        {
                            <div asp-validation-summary="ModelOnly" class="alert alert-danger mb-3" role="alert"></div>
                        }

                        <!-- Tipo de Umbral -->
                        <div class="mb-3">
                            <label asp-for="TipoUmbral" class="form-label"></label>
                            <select asp-for="TipoUmbral"
                                    class="form-select"
                                    asp-items="Html.GetEnumSelectList<TipoUmbral>()"
                                    aria-describedby="tipoUmbralHelp"
                                    required>
                                <option value="">Seleccione el tipo de operación...</option>
                            </select>
                            <span asp-validation-for="TipoUmbral" class="text-danger small"></span>
                            <div class="form-text" id="tipoUmbralHelp">
                                Seleccione el tipo de límite que necesita exceder.
                            </div>
                        </div>

                        <!-- Tipo de Operación -->
                        <div class="mb-3">
                            <label asp-for="TipoOperacion" class="form-label"></label>
                            <select asp-for="TipoOperacion" class="form-select" required>
                                <option value="">Seleccione...</option>
                                <option value="Venta">Venta</option>
                                <option value="Compra">Compra</option>
                                <option value="Crédito">Crédito</option>
                                <option value="Descuento">Descuento</option>
                                <option value="Ajuste">Ajuste de Stock</option>
                                <option value="Otro">Otro</option>
                            </select>
                            <span asp-validation-for="TipoOperacion" class="text-danger small"></span>
                        </div>

                        <!-- Referencia -->
                        <div class="mb-3">
                            <label asp-for="ReferenciaOperacionId" class="form-label"></label>
                            <input asp-for="ReferenciaOperacionId"
                                   class="form-control"
                                   type="number"
                                   min="0"
                                   inputmode="numeric"
                                   aria-describedby="referenciaHelp"
                                   placeholder="ID de la operación (opcional)" />
                            <span asp-validation-for="ReferenciaOperacionId" class="text-danger small"></span>
                            <div class="form-text" id="referenciaHelp">
                                Si esta solicitud está relacionada con una operación específica, ingrese su ID.
                            </div>
                        </div>

                        <!-- Valores -->
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label asp-for="ValorSolicitado" class="form-label"></label>
                                <div class="input-group">
                                    <span class="input-group-text">$</span>
                                    <input asp-for="ValorSolicitado"
                                           class="form-control"
                                           type="number"
                                           step="0.01"
                                           min="0"
                                           inputmode="decimal"
                                           aria-describedby="valorSolicitadoHelp"
                                           required
                                           placeholder="0.00" />
                                </div>
                                <span asp-validation-for="ValorSolicitado" class="text-danger small d-block"></span>
                                <div class="form-text" id="valorSolicitadoHelp">
                                    Valor que necesita autorizar.
                                </div>
                            </div>

                            <div class="col-md-6">
                                <label asp-for="ValorPermitido" class="form-label"></label>
                                <div class="input-group">
                                    <span class="input-group-text">$</span>
                                    <input asp-for="ValorPermitido"
                                           class="form-control"
                                           type="number"
                                           step="0.01"
                                           min="0"
                                           inputmode="decimal"
                                           aria-describedby="valorPermitidoHelp"
                                           placeholder="0.00" />
                                </div>
                                <span asp-validation-for="ValorPermitido" class="text-danger small d-block"></span>
                                <div class="form-text" id="valorPermitidoHelp">
                                    Valor máximo permitido para su rol (si lo conoce).
                                </div>
                            </div>
                        </div>

                        <!-- Diferencia calculada -->
                        <div class="alert alert-warning mt-3 mb-3 d-none" id="alertDiferencia" role="status" aria-live="polite">
                            <i class="bi bi-calculator me-2" aria-hidden="true"></i>
                            <strong>Exceso solicitado:</strong>
                            <span id="diferencia">$0.00</span>
                        </div>

                        <!-- Justificación -->
                        <div class="mb-3">
                            <label asp-for="Justificacion" class="form-label"></label>
                            <textarea asp-for="Justificacion"
                                      class="form-control"
                                      rows="4"
                                      minlength="20"
                                      maxlength="1000"
                                      aria-describedby="justificacionHelp"
                                      required
                                      placeholder="Explique detalladamente por qué necesita esta autorización..."></textarea>
                            <span asp-validation-for="Justificacion" class="text-danger small"></span>
                            <div class="form-text" id="justificacionHelp">
                                Mínimo 20 caracteres. Sea específico sobre el motivo de la solicitud.
                                <span id="charCount" class="float-end" aria-live="polite">0/1000</span>
                            </div>
                        </div>

                        <!-- Botones -->
                        <div class="d-flex justify-content-end gap-2 mt-4">
                            <a href="@backUrl" class="btn btn-outline-secondary">
                                <i class="bi bi-x-lg me-1" aria-hidden="true"></i>Cancelar
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-send me-1" aria-hidden="true"></i>Enviar solicitud
                            </button>
                        </div>
                    </form>

                </div>
            </div>

        </div>

        <!-- Ayuda -->
        <div class="col-12 col-lg-4 col-xl-6">
            <div class="card card-animate bg-light">
                <div class="card-header">
                    <h2 class="h6 mb-0">
                        <i class="bi bi-question-circle me-2" aria-hidden="true"></i>Ayuda
                    </h2>
                </div>
                <div class="card-body small">
                    <h3 class="h6 fw-semibold">¿Cuándo solicitar autorización?</h3>
                    <ul class="mb-3">
                        <li>Cuando el descuento excede su límite permitido</li>
                        <li>Cuando el monto de venta supera el máximo autorizado</li>
                        <li>Cuando necesita aprobar un crédito mayor al permitido</li>
                        <li>Para cualquier operación que exceda los umbrales de su rol</li>
                    </ul>

                    <h3 class="h6 fw-semibold">Tipos de umbral</h3>
                    <ul class="mb-3">
                        <li><strong>Descuento en Venta:</strong> % máximo de descuento</li>
                        <li><strong>Monto Total Venta:</strong> valor máximo por venta</li>
                        <li><strong>Monto Crédito:</strong> crédito máximo a aprobar</li>
                        <li><strong>Descuento en Compra:</strong> % descuento en compras</li>
                    </ul>

                    <h3 class="h6 fw-semibold">Recomendaciones</h3>
                    <ul class="mb-0">
                        <li>Sea claro y específico en la justificación</li>
                        <li>Incluya el contexto del cliente o situación</li>
                        <li>Mencione si es un caso excepcional o recurrente</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script src="~/js/autorizacion-crear-solicitud.js" asp-append-version="true"></script>
}

@* 
MEJORAS (solo HTML):
- Se agregaron aria-describedby y aria-live para ayudas/contadores y feedback dinámico (mejora accesibilidad).
- Se agregó role="alert"/"status" a bloques de alertas/estado.
- Se ajustaron headings del panel de ayuda para jerarquía semántica consistente.
- Se agregaron inputmode para mejorar UX en mobile/teclado.
- Líneas: pueden aumentar por atributos ARIA/ayudas; no puedo confirmar el número exacto sin comparar contra tu archivo en el repo. 
*@
